#!/usr/bin/pulseaudio -nF
# fix volume not being adjustable
.ifexists module-droid-card.so
load-module module-droid-card rate=48000 voice_virtual_stream=true use_legacy_stream_set_parameters=true
.endif
